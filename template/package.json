{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "release": "npm run build && num run release"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^1.17.1",
    "tslint": "^5.16.0"
  }
}
